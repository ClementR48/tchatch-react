import React from 'react';

const Form = () => {
  return (
    <form className="form">
      <input type="text" />
      <button type="submit"> Envoyer </button>
    </form>
  );
};

export default Form;